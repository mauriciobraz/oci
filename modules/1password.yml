type: shell
name: 1password-systemd-units

commands:
  - chmod +x /usr/bin/vos-opt-symlink
  - systemctl enable vos-opt-symlink.service
  - ln -s /usr/lib/systemd/user/1password.service /etc/systemd/system/multi-user.target.wants/1password.service
  - ln -s /usr/lib/systemd/user/op-browser-support.service /etc/systemd/system/multi-user.target.wants/op-browser-support.service

modules:
  - type: apt
    name: 1password-install

    source:
      packages:
        - 1password
        - 1password-cli

      modules:
        - type: shell
          name: 1password-repo-setup
          commands:
            - curl -sS https://downloads.1password.com/linux/keys/1password.asc | gpg --dearmor --output /usr/share/keyrings/1password-archive-keyring.gpg
            - echo 'deb [arch=amd64 signed-by=/usr/share/keyrings/1password-archive-keyring.gpg] https://downloads.1password.com/linux/debian/amd64 stable main' | tee /etc/apt/sources.list.d/1password.list
            - apt update

        - type: shell
          name: 1password-signing-policy
          commands:
            - mkdir -p /etc/debsig/policies/AC2D62742012EA22/
            - curl -sS https://downloads.1password.com/linux/debian/debsig/1password.pol | tee /etc/debsig/policies/AC2D62742012EA22/1password.pol

            - mkdir -p /usr/share/debsig/keyrings/AC2D62742012EA22
            - curl -sS https://downloads.1password.com/linux/keys/1password.asc | gpg --dearmor --output /usr/share/debsig/keyrings/AC2D62742012EA22/debsig.gpg
